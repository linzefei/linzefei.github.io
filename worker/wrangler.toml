name = "linzefei-word-worker"
main = "index.js"
compatibility_date = "2024-09-23"

# ── 公开变量（可以写在这里，不需要保密）──────────────────────────────────────
[vars]
SUPABASE_URL   = "https://dibqibhjwoogkbdqsrcc.supabase.co"
ALLOWED_ORIGIN = "https://linzefei.github.io,https://linzefei.com"

# ── 私密变量（不写在这里！用命令行设置）────────────────────────────────────
#
# 部署前先执行：
#   npx wrangler secret put TURNSTILE_SECRET
#   npx wrangler secret put SUPABASE_SERVICE_ROLE
#
# 系统会提示你粘贴对应的 key，不会出现在任何文件里。
